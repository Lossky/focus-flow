# Focus Flow 0.1.4

## 新功能

- Header 显示当前版本号徽章。
- 名言系统全面重做：
  - 替换自动拼接鸡汤为 20 条真实名人名言（苏格拉底、老子、尼采、乔布斯等），每条带作者署名。
  - 名言卡片改为全屏 DVD 屏保式自动弹跳，碰到边缘反弹。
  - 切换间隔从 12 秒放慢到 30 秒。

## 架构优化

- 新增 `src/hooks/use-pomodoro.ts`，将番茄钟状态、倒计时、通知、休息提醒逻辑从 page.tsx 中抽离为独立 hook。
- page.tsx 减少约 80 行番茄钟相关代码。
