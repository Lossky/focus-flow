# Focus Flow 0.1.6

## 专注页面重做

- 数字计时器替换为 SVG 圆环进度条，实时显示剩余时间比例。
- 圆环带呼吸动画，运行时微微缩放，营造沉浸感。
- 完成时圆环变绿，文字显示"做得好！"。
- 顶部状态标签带脉冲动画（● 专注中 / ✓ 已完成 / ◉ 已暂停），旁边加跳动爱心。
- 背景加两个模糊光晕（琥珀色 + 玫瑰色），完成时琥珀色变绿。
- 按钮加阴影和 hover 放大效果。
- 专注页面抽为独立 `FocusSession` 组件，page.tsx 减少约 130 行。

## 角落小窗升级

- 标题栏加像素动画：专注模式显示跳动爱心，主线模式显示动画小猫。
- 计时器换成圆环进度条 + 呼吸动画，与主页面风格统一。
- 背景加模糊光晕。
- 空状态显示跳动爱心。
- 任务卡片加 hover 过渡效果。
- 主线列表头部加小猫动画和任务计数。

## 验证

- `npm run lint`：通过，0 errors。
- `npm run build`：通过。
- `npm run tauri:build`：通过。
- 产物：`Focus Flow_0.1.6_aarch64.dmg`。
